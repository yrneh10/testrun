name: testtf yml

on:
  push:
    branches:
      - main

jobs:
  call-security-scan:
    uses: yrneh10/testrun/.github/workflows/nodereusable.yml@main
    secrets:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
